{% import "_includes/forms" as forms %}

{{ forms.textField({
    id: id ~ 'visitsCount',
    class: 'visitsCount',
    name: name ~ '[visitsCount]',
    value: value.visitsCount,
    errors: value.getErrors('visitsCount'),
    required: false,
    disabled: true
}) }}
